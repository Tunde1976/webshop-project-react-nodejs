import { createContext } from "react";

const OrderErrorMessages = createContext({});

export const OrderShippingMessages = createContext({});

export const ValidOrderForm = createContext(true);

export default OrderErrorMessages;