import { createContext } from "react";

export const BillingAddress = createContext({});

export const OrderComment = createContext({});

export const ShippingAddress = createContext({});

export const OrderTotal = createContext(0);

export const OrderCheckbox = createContext(true);